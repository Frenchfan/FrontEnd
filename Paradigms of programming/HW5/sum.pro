Программа на языке Prolog, которая вычисляет сумму элементов списка:
sum_list([], 0).
sum_list([H|T], Sum) :-
sum_list(T, Sum0),
Sum is H + Sum0.

Можно использовать эту программу следующим образом:
```
?- sum_list([1,2,3,4,5], Sum).
